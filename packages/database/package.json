{
    "name": "database",
    "version": "0.0.0",
    "main": "./index.ts",
    "types": "./index.ts",
    "dependencies": {
        "@prisma/client": "^5.2.0"
    },
    "devDependencies": {
        "prisma": "^5.2.0"
    },
    "scripts": {
        "db:generate": "prisma generate",
        "db:push": "prisma db push --skip-generate"
    },
    "pipeline": {
        "db:generate": {
            "cache": false
        },
        "db:push": {
            "cache": false
        }
    }
}